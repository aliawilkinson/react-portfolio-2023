import React from 'react'
import css from './hero.module.scss'

const Hero = () => {
    return (
        <section className={css.wrapper}>
            Hero
        </section>
    )
}

export default Hero